# 视觉与交互验收

验收日期：2026-07-26  
参考站点：`https://shnlfriberg.online`  
实现地址：`http://127.0.0.1:5173`

## 对照范围

在开始 UI 改造前，已从参考站点捕获主页、规则、难度、空对局、已有猜测、选手查询及深色主题基准。实现完成后使用相同状态和视口重拍，并将参考图与实现图合并为同一张对照图后逐项检查。

- 桌面视口：1280 × 720
- 手机视口：390 × 844
- 参考图：`source-evidence/*.png`
- 实现图：`design-qa-artifacts/01-*.png` 至 `11-*.png`
- 同屏对照：`design-qa-artifacts/compare-*.png`，左侧为参考站，右侧为英雄联盟版

已对照状态：

1. 浅色主页、深色主页和手机主页
2. 游戏规则弹窗
3. 单人难度选择
4. 空对局和已有一次猜测的桌面反馈表
5. 已有一次猜测的手机字段卡片
6. 查询空态、Faker 查询结果及手机查询结果

## 发现与修复

- P1：自动补全输入框此前依赖浏览器隐式表单提交，键盘回车路径不够明确。已增加显式 `Enter` 处理，并增加回归测试；浏览器中仅用键盘已可完成 Faker 别名查询。
- P1：参考站手机对局仍会横向裁切桌面表格。英雄联盟版按需求改为带字段标签、文字反馈和方向说明的纵向卡片；390px 视口无横向溢出。
- P2：首轮实现的主页舞台高度、顶部留白、标题比例和规则入口均小于参考站。已按同视口对照调整桌面与手机的舞台高度、间距、标题、规则按钮和规则弹窗宽度。
- P2：顶部按钮与装饰伪元素缺少明确层级。已为标题与按钮组补充层级，避免主题控件在复杂合成层中被遮盖。
- 截图工具的 `fullPage` 模式会遗漏单独合成的顶部控件；最终验收统一使用精确视口截图。该问题只影响 QA 截图方式，不影响浏览器中的页面呈现。

修复后未发现未关闭的 P0、P1 或 P2 视觉问题。

## 交互与无障碍

- 首页导航、规则弹窗、难度切换、开始/恢复游戏、猜测、查看答案、主题切换和选手查询均已实机走通。
- 所有核心流程均有可访问名称；弹窗使用对话框语义，自动补全使用 combobox/listbox/option 语义，异步结果通过 live region 通知。
- 九字段反馈同时提供颜色、文字和方向，不以颜色作为唯一信息来源。
- 手机猜测记录使用语义化字段列表，键盘可完成输入、选择和提交。
- 390px 下主页、查询结果和游戏页的文档及内容容器均满足 `scrollWidth <= clientWidth`。
- 浅色与深色主题均完成桌面视觉检查。

## 最终结果

passed
